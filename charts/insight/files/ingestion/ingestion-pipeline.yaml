apiVersion: argoproj.io/v1alpha1
kind: WorkflowTemplate
metadata:
  name: ingestion-pipeline
  labels:
    app.kubernetes.io/part-of: ingestion
spec:
  entrypoint: pipeline
  templates:
    - name: pipeline
      inputs:
        parameters:
          - name: connection_id
          - name: dbt_select
            default: "tag:silver"
          - name: toolbox_image
            default: "ghcr.io/cyberfabric/insight-toolbox:latest"
          - name: airbyte_url
            default: "__AIRBYTE_URL__"
          - name: clickhouse_host
            default: "__CLICKHOUSE_FQDN__"
          - name: clickhouse_port
            default: "__CLICKHOUSE_PORT__"
      dag:
        tasks:
          - name: sync
            templateRef:
              name: airbyte-sync
              template: sync
            arguments:
              parameters:
                - name: connection_id
                  value: "{{inputs.parameters.connection_id}}"
                - name: airbyte_url
                  value: "{{inputs.parameters.airbyte_url}}"
          - name: transform
            depends: sync
            templateRef:
              name: dbt-run
              template: run
            arguments:
              parameters:
                - name: dbt_select
                  value: "{{inputs.parameters.dbt_select}}"
                - name: toolbox_image
                  value: "{{inputs.parameters.toolbox_image}}"
                - name: clickhouse_host
                  value: "{{inputs.parameters.clickhouse_host}}"
                - name: clickhouse_port
                  value: "{{inputs.parameters.clickhouse_port}}"
