##
## Frontend — local development overrides
##
## Base values: src/frontend/helm/values.yaml
##

replicaCount: 1

ingress:
  enabled: false        # access via port-forward locally

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 128Mi
