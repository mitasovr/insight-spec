name: ingestion
version: "1.0.0"
config-version: 2

profile: ingestion

model-paths:
  - silver
  - ../connectors

macro-paths:
  - macros

seed-paths:
  - seeds

test-paths:
  - tests

clean-targets:
  - target
  - dbt_packages
