# Tenant configs with real credentials (only .yaml.example is tracked)
connections/*.yaml
!connections/*.yaml.example

# Airbyte state (generated by scripts, instance-specific)
connections/.airbyte-state.yaml
connections/.state/

# Generated schemas and catalogs (can be regenerated)
connectors/**/schemas/
connectors/**/state.json

# Generated CronWorkflows (created by sync-flows.sh from .tpl templates)
# Templates are tracked; tenant dirs are generated at deploy time
workflows/*/
!workflows/templates/
!workflows/schedules/
