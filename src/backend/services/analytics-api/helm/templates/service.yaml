apiVersion: v1
kind: Service
metadata:
  name: {{ include "insight-analytics-api.fullname" . }}
  labels:
    {{- include "insight-analytics-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "insight-analytics-api.selectorLabels" . | nindent 4 }}
